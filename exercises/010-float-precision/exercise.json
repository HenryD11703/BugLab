{
  "title": "Almost Equal",
  "difficulty": "medium",
  "tags": ["floats", "precision", "comparison", "gotcha"],
  "description": "The function `is_right_triangle(a, b, c)` returns `True` if a triangle with sides `a`, `b`, `c` is a right triangle. The sides can be given in any order.\n\nIt works perfectly for integer sides like `3, 4, 5`. But with decimal values like `0.3, 0.4, 0.5` — a perfectly valid right triangle — it **returns `False`**.\n\nThe formula is correct. The math is correct. So why does it fail?",
  "hints": [
    "Try `is_right_triangle(3, 4, 5)` (should be `True`) and `is_right_triangle(0.3, 0.4, 0.5)` (also a valid right triangle). What does the second call return?",
    "Floating-point numbers can't represent most decimal fractions exactly. `0.3 ** 2 + 0.4 ** 2` doesn't produce exactly `0.25` in binary floating-point — it produces `0.25000000000000003`. So `== 0.25` fails.",
    "Instead of exact equality, check that the difference is *small enough*: `abs(sides[0]**2 + sides[1]**2 - sides[2]**2) < 1e-9`. Never compare floats with `==`."
  ],
  "tests": [
    {
      "description": "classic 3-4-5 right triangle (integers, works either way)",
      "input": "is_right_triangle(3, 4, 5)",
      "expectedOutput": "True"
    },
    {
      "description": "0.3-0.4-0.5 right triangle — float precision trap",
      "input": "is_right_triangle(0.3, 0.4, 0.5)",
      "expectedOutput": "True"
    },
    {
      "description": "not a right triangle",
      "input": "is_right_triangle(1, 2, 3)",
      "expectedOutput": "False"
    },
    {
      "description": "5-12-13 right triangle (integers)",
      "input": "is_right_triangle(5, 12, 13)",
      "expectedOutput": "True"
    }
  ],
  "requirements": {}
}
